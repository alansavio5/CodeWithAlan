# array >>>>>> an array is a data structure that stores values same datatype, 
#                   where a list stores values of different datatype

import numpy as np 

new_list = [1,2,3,4]
arr = np.array(new_list)
